---
import {
  schemaLocalBusiness, schemaFAQ, schemaHowTo, schemaPerson,
  schemaWebSite, schemaProfessionalService, schemaBreadcrumb,
} from "../data/schemas";
---
<script type="application/ld+json" is:inline set:html={JSON.stringify(schemaWebSite)} />
<script type="application/ld+json" is:inline set:html={JSON.stringify(schemaPerson)} />
<script type="application/ld+json" is:inline set:html={JSON.stringify(schemaProfessionalService)} />
<script type="application/ld+json" is:inline set:html={JSON.stringify(schemaLocalBusiness)} />
<script type="application/ld+json" is:inline set:html={JSON.stringify(schemaBreadcrumb)} />
<script type="application/ld+json" is:inline set:html={JSON.stringify(schemaFAQ)} />
<script type="application/ld+json" is:inline set:html={JSON.stringify(schemaHowTo)} />
